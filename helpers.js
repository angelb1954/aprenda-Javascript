export function getDescription(text) {
    console.log(text); export function getDescription(text) {
    console.log(text); // write something in the BROWSER and see it in the console
    return input.substring(0,10);